<% $max_key_len = $bginfo::displayed_facts.reduce(0) |$m, $k| { max($m, $k.length) } -%>
<% $bginfo::displayed_facts.each |$element| { -%>
  <%
    $fact_parts = $element.split('[.]')
    if $fact_parts[0] == 'trusted' {
      $trusted_parts = $fact_parts[1, $fact_parts.length - 1]
      $fact_value    = dig($trusted, *$trusted_parts)
    } else {
      $fact_value = dig($facts, *$fact_parts)
    }
  -%>
<%= sprintf("%-*s\t%s", $max_key_len + 1, "${element}:", $fact_value) %>
<% } -%>
